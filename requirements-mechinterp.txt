# Install a CUDA-compatible PyTorch wheel or use a PyTorch GPU image first.
torch>=2.7
torchvision>=0.22
# Exact version used for the frozen Qwen3.6-27B mechanistic runs. 5.15 moved
# the Gated DeltaNet recurrence helper and is not intervention-compatible.
transformers==5.14.1
accelerate>=1.6
safetensors>=0.5
huggingface-hub>=0.30
numpy>=2.0
scipy>=1.14
scikit-learn>=1.6
matplotlib>=3.9
tqdm>=4.67
